import React, { ReactElement } from 'react';
import Layout from '@components/MyPage/Layout';

const certificates = (): ReactElement => {
	return <Layout>certificates</Layout>;
};

export default certificates;
